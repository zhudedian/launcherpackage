<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/home_small_view_unselect_bg"
    android:orientation="vertical">


    <ImageView
        android:id="@+id/shortcut_folder_thumbnail_grid"
        android:layout_width="120dp"
        android:layout_height="110dp"
        android:scaleType="fitXY"
        android:visibility="gone"
        android:layout_marginTop="25dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/folder_thumbnail_background" />
    <ImageView
        android:id="@+id/shortcut_folder_thumbnail_grid2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:layout_margin="5dp"
        android:layout_centerHorizontal="true"
         />
    <TextView
        android:id="@+id/shortcut_folder_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dp"
        android:textColor="@color/black"
        android:background="#53fefdfd"
        android:text="@string/favorite"
        android:textSize="25sp"/>

</RelativeLayout>